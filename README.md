# WebTester-HTTP-HTTPS-Server-Analysis-Tool
WebTester is a low-level network diagnostic tool built in Python that analyzes web servers by directly interacting with them using raw sockets and TLS (SSL) without relying on high-level libraries like requests or urllib.
